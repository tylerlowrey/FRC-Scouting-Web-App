import React from "react";

const TeamPage = () => {

    let navigationItems = [
        { name: "Teams List", href: "#"},
        { name: "Advanced Search", href: "#"},
        { name: "Settings", href: "#"},
        { name: "Logout", href: "/logout"}
    ];
};